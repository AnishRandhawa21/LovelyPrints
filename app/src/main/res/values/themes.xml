<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.LovelyPrints"
        parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Match your app's actual background color -->
        <item name="android:windowBackground">#151419</item>

        <!-- Prevent white flash during activity transitions -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@android:transition/no_transition</item>
        <item name="android:windowExitTransition">@android:transition/no_transition</item>

    </style>

</resources>